# Railway Build Configuration

- Added `railway.json` so Railway builds from the repo root.
- Build pipeline now installs Python deps and builds the Vite frontend before launch.
- Keep `Procfile` for local compatibility; start command is mirrored in `railway.json`.

